# Copy to .env (or just export) — sensible defaults are baked in but rotate
# JWT_SECRET before deploying anywhere real.

PORT=3000
JWT_SECRET=dev-only-secret-rotate-in-prod
JWT_EXPIRES=12h
DB_PATH=./data/spma.sqlite
